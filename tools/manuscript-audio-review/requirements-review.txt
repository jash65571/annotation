numpy==2.4.6
opensmile==2.6.0
soundfile==0.14.0
torch==2.13.0
transformers==5.15.1
# silero-vad (optional VAD fallback) is installed best-effort by
# setup_windows.ps1, NOT here, so its failure cannot break the core stack.
