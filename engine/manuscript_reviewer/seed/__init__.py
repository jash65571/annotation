"""Seed subsystem (Phase 4): immutable snapshot, robust parser, atomic claims,
task feedback, and structural comparison against deterministic media truth.

The original seed is immutable evidence: it is never normalized, repaired, or
reordered. Parsing produces a separate, recoverable representation.
"""
