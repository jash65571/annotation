{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main cockpit window: core IPC/events, native open/save dialogs, write-only clipboard, and nothing else. No fs scope, no shell, no HTTP — run files are served through validated Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "clipboard-manager:allow-write-text"
  ]
}
